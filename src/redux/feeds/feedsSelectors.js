export const getLoading = (state) => state.feeds.loading;

export const getTrendingFeeds = (state) => state.feeds.trendingFeeds;

export const getUserInfo = (state) => state.feeds.info;

export const getUserFeeds = (state) => state.feeds.userFeeds;

export const getError = (state) => state.feeds.error;
